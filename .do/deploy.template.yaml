spec:
  name: d3-viz-backend
  services:
    - name: web
      git:
        branch: main
        repo_clone_url: https://github.com/digitalocean/D3_Viz_backend.git
      envs:
        - key: APP_URL
          value: "http://vizbyd3.com/"
          type: SECRET
